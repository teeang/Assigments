package Lab3;

import java.util.Scanner;

public class Expressions {
    public static void main(String[] args) {

        double a = 5;
        double b = 7;
        double c = 8;
        double x = 3;
        double y = 2;
        double R = 10;

        System.out.println("Math.sqrt(b*b + 4*a*c) = " + Math.sqrt(b*b + 4*a*c));
        System.out.println("Math.sqrt (x + 4 * (Math.pow(y, 3) = " + Math.sqrt (x + 4 * (Math.pow(y, 3))));
        System.out.println("Math.cbrt (x*y) = " + Math.cbrt (x*y) );
        System.out.println(("area = " + Math.sqrt(R) * Math.PI));


    }

    }

